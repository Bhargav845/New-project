(function($) {
     var typed = new Typed("span.text-rotate", {
          strings: ["i'm the one", "ok we can do it"],
          typespeed: 100,
          backspeed: 100,
          fadeout: false,
          smartbackspace: true,
          loop: true
     });
})(jQuery);

(function($) {
     var typed = new Typed('a.text-rotate', {
          strings: ["Every thing is possible"],
          typespeed: 100,
          backspeed: 100,
          fadeout: false,
          smartbackspace:true,
          loop: true
     });
})(jQuery);

